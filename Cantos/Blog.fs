namespace Cantos
//
//type BlogConfig = { Title:string; PostsPath:InPath; }
//
//type Blog (blogConfig, siteConfig:SiteConfig) =
//    interface IPlugin with member x.Name = sprintf "BlogPlugin for path %s" <| siteConfig.GetFullPathString(blogConfig.PostsPath)

